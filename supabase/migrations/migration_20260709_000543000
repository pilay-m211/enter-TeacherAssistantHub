-- Add written feedback per grade entry
alter table grade_ledger add column if not exists feedback text;

-- Optional custom labels for rubric-style multi-criterion assignments (question_count > 1)
alter table files add column if not exists question_labels text[];
